#include "main.h"
/**
 * mul - this function checks for a digit (0 through).
 * @a: a - Variable letter
 * @b: b - Variable letter
 * Return: Always 0.
*/
int mul(int a, int b)
{
int mul;

mul = a * b;
return (mul);
}
